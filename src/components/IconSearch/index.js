import styled from 'styled-components';
import icon from '../../assets/images/icon-search.svg';

const IconSearch = styled.img.attrs({
  src: icon,
  alt: 'Ícone de lupa que representa o recurso de busca no site',
})`
  color: red;
`;

export default IconSearch;
